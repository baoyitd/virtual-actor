# v0.5.0 测试用例

版本：v0.5.0 | 日期：2026-06-11 | 状态：Self-Tested

> 本文档用于支撑 `v0.5` 实现验收、测试方独立复核与最终用户测试，不代表当前版本已 `Accepted`。

## US-22：用户可通过 AI 或手工方式创建角色规格草案

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A201` | 创建角色草案含 `main_duty_cluster` | 版本字段正确保存 |
| `A202` | 创建角色草案含 `point_of_view` | 新 `L1` 主路径字段正确保存 |
| `A203` | 创建角色草案默认 `output_mode = freeform` | 默认输出方式正确落入草案 |
| `A204` | draft 角色可不完整保存 | 创建阶段不因未补齐全部发布要求而失败 |
| `A205` | AI 草案接口返回结构化草案 | 至少返回 `L1`、消费契约、`output_mode` 建议 |
| `A206` | AI 草案不直接落库 | 必须用户确认后才生成角色 |
| `A207` | AI 草案不直接绑定真实知识 | `knowledge_refs` 仍由人确认 |
| `A208` | AI 草案不直接绑定数据资产 | `data_asset_bindings` 仍由人确认 |
| `A209` | AI 草案可建议 `structured` 路径 | 若建议结构化输出，则一并返回 `output_type / output_schema` 初稿 |
| `A210` | 手工创建与 AI 创建共存 | 两条入口都进入同一编辑骨架 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F201` | 新建页信息架构 | 先“这个角色是什么”，再“怎么用 / 为什么可信”，最后治理侧 |
| `F202` | AI / 手工入口 | 两条入口同构，不是两套割裂页面 |
| `F203` | AI 草案侧边摘要 | 展示结构化草案，不是纯文案块 |
| `F204` | AI 草案填入工作区 | 用户可整体填入或逐段采用 |
| `F205` | 关键字段提示 | `placeholder` 使用模板句式，不是空白文本框 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H201` | 用一段业务描述生成角色草案 | 用户能看懂草案结构并继续编辑 |
| `H202` | 纯手工创建角色草案 | 不依赖口头解释即可完成基础创建 |

## US-23：用户可在统一工作区完成 `L1-L4`

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A211` | 查询工作区摘要数据 | 返回缺失项、完成进度、发布就绪度所需数据 |
| `A212` | `L2` 支持无知识绑定 | `knowledge_refs` 为空时状态合法且明确 |
| `A213` | `L2` 有知识绑定后可编辑 `knowledge_boundary` | 有绑定时边界可编辑并保存 |
| `A214` | `L3` 支持 `0~多条` 数据资产绑定 | `data_asset_bindings` 可为空、可多选 |
| `A215` | 角色页只接受管理员配置的数据资产项 | 不允许现场创建临时数据资产 |
| `A216` | `L3` 空状态返回明确文案 | 显示 `当前未授权结构化业务数据` |
| `A217` | `L4` 默认为 `freeform` | 无结构化配置时角色仍是合法草案 |
| `A218` | 切到 `structured` 后要求 `output_type` | 条件字段正确出现 |
| `A219` | `structured` 路径要求 `output_schema` | 基于模板扩展，不允许空白 schema |
| `A220` | `model_binding` 默认继承系统值 | 未显式覆盖时返回继承态 |
| `A221` | 工作区支持分段保存 | 用户可按区块保存，不需整页一次提交 |
| `A287` | 绑定真实知识后测试 / 消费结果受其影响 | 针对依赖知识的问题，结果能体现已绑定知识参与，而不是只在详情页显示“已绑定” |
| `A288` | 未绑定真实知识时测试 / 消费结果如实降级 | 不伪装为已引用真实知识，限制说明与结果一致 |
| `A289` | `knowledge_boundary` 在运行时真实生效 | 超出声明边界的输入以限制 / 降级语义表达，不把未覆盖内容包装成已覆盖结论 |
| `A290` | 绑定数据资产后测试 / 消费结果走平台查询层 | 不是只回显资产摘要，结果能体现真实取数链路 |
| `A291` | 不同数据资产绑定导致结果差异 | 同一任务下可用数据范围或结果内容会随绑定变化 |
| `A292` | 无数据绑定或资产不可用时结果如实降级 | 不隐式取数，不把未授权数据包装成已授权能力 |
| `A293` | `freeform` 路径运行时返回自由输出 | 结果形态与自由输出契约一致，不强行附会结构化骨架 |
| `A294` | `structured` 路径运行时遵循 `output_type / output_schema` | 返回 `structured_result`，字段完整且与契约一致 |
| `A295` | 切换 `output_mode` 后运行结果形态切换 | 同一角色在 `freeform / structured` 两种配置下得到不同且可解释的结果形态 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F206` | 左侧章节导航 | 用户知道当前在编辑哪一层 |
| `F207` | `L1` 主路径收口 | `name / bio / 核心职责 / 分析视角` 处于主路径 |
| `F208` | `L2` 双路径 | 同时支持“绑定真实知识”和“暂不绑定真实知识” |
| `F209` | `L3` 页面表达 | 只出现数据资产选择与摘要，不再出现旧能力说明字段 |
| `F210` | `L4` 页面表达 | 先选 `freeform / structured`，再决定是否展示模板与 schema |
| `F211` | 高级项弱化 | `decision_style / identity_background / speaking_style / model_binding` 默认折叠 |
| `F212` | 工作区进度摘要 | 始终能看见定义层 / 契约层 / 治理层缺口 |
| `F213` | 长表单风险被拆解 | 页面不再是单列瀑布式堆字段 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H203` | 从头完成一个角色定义工作区 | 用户知道每一层在表达什么 |
| `H204` | 不绑定知识完成角色草案 | 界面明确显示合法空状态 |
| `H205` | 不绑定数据能力完成角色草案 | 界面明确显示合法空状态 |
| `H218` | 绑定与不绑定知识分别执行同一任务 | 用户能观察到结果差异，并确认未绑定时是如实降级而非伪装有知识 |
| `H219` | 更换或停用数据资产后重复执行同一任务 | 数据能力变化能在结果中真实体现，而不是只改说明文案 |
| `H220` | 切换 `freeform / structured` 后重复测试 | 返回形态与配置一致，结构化路径不漂移为自由文本，反之亦然 |

## US-24：角色具备独立消费契约

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A222` | 详情接口返回消费契约字段 | `applicable_scenarios / usage_notes / support_basis_summary` 正确返回 |
| `A223` | 消费契约带系统摘要 | 知识状态、数据能力状态、输出方式、测试摘要可合成展示 |
| `A224` | 缺消费契约时状态正确 | draft 可缺失，published 不可缺失 |
| `A225` | 消费契约与角色定义并存 | 不互相覆盖或混淆 |
| `A226` | `freeform` 角色也可有完整消费契约 | 不强制要求结构化输出 |
| `A227` | 消费契约对 legacy 角色友好降级 | 缺失时返回待补齐态而非异常 |
| `A228` | 使用前预览所需数据可读取 | 前置卡数据完整 |
| `A298` | 保存当前说明卡后角色进入 `test` | `02` 的保存动作承担 `draft -> test`，不再依赖测试页切状态 |
| `A274` | 说明卡来源变化后标记`待确认更新` | `L1-L4` / 知识状态 / 数据能力状态 / 测试摘要变化时不静默覆盖当前保存版 |
| `A275` | 说明卡重新生成前沿用当前保存版 | 使用台 / 前置卡 / 外供生成读取当前保存版而非未确认草案 |
| `A276` | 说明卡处于`待确认更新`时发布 / 外供 | 被阻断，直到重新生成并保存或重新确认保存 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F214` | 使用前说明版面 | 清楚区分“怎么用”和“为什么可信” |
| `F215` | 状态引用 | 能看到知识状态、数据能力状态和输出方式 |
| `F216` | 详情页与前置卡口径一致 | 用户不会看到两套说明 |
| `F243` | `02 -> test` 状态动作 | 页面主动作明确表达“保存当前说明并进入测试”，保存后不再停留在 `draft` |
| `F235` | 说明卡更新提醒 | 来源变化后能看到`待确认更新`、重新生成与重新保存动作 |
| `F241` | 页面外显名锁定 | 页面标题、导航入口与主按钮统一使用 `使用前说明与调用预览` / `使用前说明`，不回退为 `消费契约` |
| `F242` | 说明卡主结构 | 页面以系统生成说明卡为主，轻量修订入口折叠呈现，不回退为“编辑表单优先 + 预览”结构 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H206` | 看一张角色说明卡判断是否适合任务 | 无需先消费一次即可做判断 |
| `H222` | 在 `02` 保存当前说明并进入测试 | 用户能感知这是进入测试态的动作，而不是仍停留在草稿 |
| `H213` | 修改角色来源后再看说明卡 | 用户知道说明卡需要重新确认，而不是被静默改写 |
| `H217` | 走查使用前说明页主结构 | 用户先看到说明卡，再按需展开轻量修订，不会误以为这是第二个大表单页 |

## US-25：发布门禁按现有机制补充新检查项

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A229` | 缺 `main_duty_cluster` 发布 | 阻断 |
| `A230` | 缺 `applicable_scenarios` 发布 | 阻断 |
| `A231` | 缺 `usage_notes` 发布 | 阻断 |
| `A232` | 缺 `support_basis_summary` 发布 | 阻断 |
| `A233` | `output_mode = freeform` 且无 `output_type` 发布 | 合法，不因结构化字段缺失被阻断 |
| `A234` | `output_mode = structured` 但缺 `output_type` 发布 | 阻断 |
| `A235` | `output_mode = structured` 但缺 `output_schema` 发布 | 阻断 |
| `A236` | 缺 `enterprise_role_mapping` 发布 | 软提示，不阻断 |
| `A237` | 新旧门禁都满足时发布 | 发布成功 |
| `A277` | 缺 `owner` 进入可供他人消费 / 发布 | 阻断 |
| `A278` | 缺 `business_domain` 进入可供他人消费 / 发布 | 阻断 |
| `A279` | 缺 `category` 进入可供他人消费 / 发布 | 阻断 |
| `A280` | 仅缺 `enterprise_role_mapping` 或 `point_of_view` | 只给软提示，不阻断发布 |
| `A299` | `04` 承接归档动作 | `archive` 由治理页承接，归档后不再属于活跃复用链 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F217` | 发布检查清单 | 明确区分硬门禁和软提示 |
| `F218` | 条件门禁表达 | 只有 `structured` 路径下才展示结构化阻断项 |
| `F219` | 缺失项回跳 | 用户可直接回到对应区块补齐 |
| `F236` | 治理主路径阻断可见 | `owner / business_domain / category` 缺失时能在共享 / 发布前形成明确阻断 |
| `F244` | `04` 生命周期动作 | 页面只承接发布 / 归档，不再出现“进入试用”类动作 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H207` | 尝试发布未补齐角色 | 用户能看懂为什么不能发布以及下一步去哪补 |
| `H214` | 缺治理主路径尝试共享 / 发布 | 用户能理解这不是软提示，而是必须补齐的资产治理要求 |

## US-26：消费者可在使用前理解角色

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A238` | 前置卡接口含角色摘要 | 返回 `bio / main_duty_cluster / applicable_scenarios` |
| `A239` | 前置卡接口含消费契约摘要 | 返回 `usage_notes / support_basis_summary` |
| `A240` | 前置卡接口含知识状态 | 已绑定和未绑定状态都正确表达 |
| `A241` | 前置卡接口含数据能力状态 | 已授权和未授权状态都正确表达 |
| `A242` | 前置卡接口含输出方式 | `freeform / structured` 可见 |
| `A243` | 无说明的角色不得进入正式可复用态 | 与发布门禁一致 |
| `A244` | 前置卡对 legacy 角色降级展示 | 可提示待补齐而不异常 |
| `A300` | 当前角色已有旧发布版本但正在编辑新草稿 | 仍可基于旧 `published version` 进入正式消费 |
| `A301` | 角色归档后正式消费 | 被阻断，不再创建新的 `usage_record` |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F220` | 使用台前置卡 | 调用前先看到说明卡 |
| `F221` | 测试台前置卡 | 与使用台口径一致 |
| `F222` | 放弃使用路径 | 用户可在此处选择换角色或返回 |
| `F245` | 正式消费入口约束 | 入口基于“存在可用已发布版本且未归档”成立，而不是误用当前编辑态判断 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H208` | 查看前置卡后决定是否继续调用 | 调用前判断成立 |

## US-27：可生成稳定外部供给物

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A245` | 未发布版本生成外供 | 阻断 |
| `A246` | 已发布版本生成 `Tool package` | 成功生成绑定 `role_version_id` 的产物 |
| `A247` | 已发布版本生成 `Skill package` | 成功生成绑定 `role_version_id` 的产物 |
| `A248` | 供给物包含消费契约 | 包含 `main_duty_cluster / usage_notes / support_basis_summary` |
| `A249` | 供给物包含知识状态和数据能力状态 | 对应状态摘要完整 |
| `A250` | 供给物包含输出方式 | 至少带 `output_mode`；若 structured，再含 `output_type` 摘要 |
| `A251` | 供给物不漂向最新版本 | 再发布新版本后旧供给物仍绑定旧版本 |
| `A252` | 供给物缺回写要求 | 阻断或视为不合格 |
| `A281` | `Tool package` 共同必需文件完整 | `package-manifest.json / role-brief.md / consume-contract.json / output-contract.json / writeback-policy.md / tool-manifest.json` 全部存在 |
| `A282` | `Skill package` 共同必需文件完整 | `package-manifest.json / role-brief.md / consume-contract.json / output-contract.json / writeback-policy.md / SKILL.md` 全部存在 |
| `A283` | `role-brief.md` 复用说明卡当前保存版 | 不生成第二套说明文本 |
| `A284` | 外供包声明数据能力非冻结 | 供给物明确写出“按当前生效配置运行，不承诺版本冻结复现” |
| `A285` | 说明或数据摘要变化后继续分发旧包 | 被标记需要重新生成，不能继续宣称为最新对外供给物 |
| `A302` | 角色归档后生成新外供 | 被阻断，不再生成新的 `Tool / Skill package` |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F223` | 外供面板入口 | 仅“存在可用已发布版本且未归档”的角色可进入主路径 |
| `F224` | `Tool / Skill` 切换 | 两种供给形态切换清晰 |
| `F225` | 供给物预览 | 能看到版本绑定和说明内容 |
| `F226` | 复制 / 下载操作 | 用户知道拿到的是供给物，不是裸 API |
| `F237` | 文件契约展示 | 外供面板明确列出共同必需文件与形态专属文件 |
| `F238` | 当前生效配置声明 | 外供面板能看见数据能力非冻结的限制说明 |
| `F239` | 旧包失效提示 | 说明或数据摘要来源变化后，页面提示需要重新生成 |
| `F246` | 归档后外供约束 | 已归档角色不能再生成新外供，但历史包与回写记录仍可查看 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H209` | 为一个已发布角色生成外供 | 用户感知到这是稳定供给物生成过程 |
| `H215` | 生成外供后再改数据能力摘要来源 | 用户能看到旧包需要重新生成，而不是默认继续有效 |

## US-28：外部环境复用后平台侧持续回写

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A253` | Tool 形态外部调用 | 平台侧形成使用记录 |
| `A254` | Skill 形态外部调用 | 平台侧形成使用记录 |
| `A255` | 写回记录保留 `role_id` | 身份未丢失 |
| `A256` | 写回记录保留 `role_version_id` | 版本未丢失 |
| `A257` | 写回记录保留 `status / boundary_status` | 治理语义未丢失 |
| `A258` | 写回记录保留 `structured_result` 或自由输出结果摘要 | 输出语义未丢失 |
| `A259` | 外部调用无回写 | 验证失败 |
| `A286` | 代表环境直用标准 | 接入方仅补环境配置即可调用，无需手工拼接 `role_id / role_version_id / consume` 契约 |
| `A296` | 外供后真实调用继续遵循同一输出契约 | `Tool / Skill` 调用结果与平台内 `consume` 一致，`freeform / structured` 语义不漂移 |
| `A297` | 外供后知识 / 数据限制继续如实生效 | 外部复用不绕过知识边界、未绑定知识说明和数据授权限制 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F227` | 平台侧查看外部使用记录 | 可识别外部来源和调用结果 |
| `F228` | 详情页 / 看板可感知外部使用 | 外部复用不是“平台失联” |
| `F240` | 代表环境接入负担 | 接入说明不要求操作者再手工拼接核心契约 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H210` | 在代表性外部环境发起一次真实调用 | 平台侧能看见回写结果 |
| `H216` | 在代表环境按包说明完成接入 | 操作者只补环境配置即可调用成功 |
| `H221` | 在代表环境对同一角色执行一次真实复用校验 | 外部结果与平台内契约一致，且未绕过知识 / 数据能力限制 |

## US-29：legacy 角色兼容与受限外供

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A260` | 打开 legacy 角色详情 | 正常返回 |
| `A261` | 编辑 legacy 角色 | 原有字段不丢失 |
| `A262` | legacy 角色显示待补齐项 | 新字段缺失时返回补齐提示 |
| `A263` | legacy 旧 `capability_*` 字段仅读取兼容 | 不再写回新主路径 |
| `A264` | 未补齐 legacy 角色发布新版本 | 被新门禁阻断 |
| `A265` | 未补齐 legacy 角色生成外供 | 被阻断 |
| `A266` | 补齐后的 legacy 角色发布 / 外供 | 可通过 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F229` | legacy 详情页提示 | 待补齐项展示明确 |
| `F230` | legacy 编辑路径 | 不出现空白区或错误映射 |
| `F231` | legacy 外供阻断提示 | 用户知道为什么不能直接外供 |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H211` | 打开旧角色并尝试补齐后外供 | 路径清晰，不像系统坏了 |

## US-30：平台管理员维护可绑定数据资产项

### 自动化 API 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `A267` | 新增数据资产项含必填字段 | `display_name / datasource_ref / database_name / table_name / scope_summary` 正确保存 |
| `A268` | 新增数据资产项缺必填字段 | 被阻断 |
| `A269` | 可选字段可为空 | `freshness / owner_team` 非必填 |
| `A270` | 停用数据资产项 | 新版本角色绑定选择器不可见 |
| `A271` | 已绑定角色读取数据资产摘要 | 能返回展示名称、表名和范围说明 |
| `A272` | 角色页不得现场创建数据资产 | 绑定接口仅接受已存在资产 ID |
| `A273` | 数据资产只读边界 | 不出现写入、执行或外部直连配置字段 |

### 程序化 UI 测试

| # | 用例 | 验证点 |
| --- | --- | --- |
| `F232` | 管理员页字段组织 | 必填和可选字段区分清楚 |
| `F233` | 状态筛选与停用联动 | 停用资产不再出现在角色页 |
| `F234` | 角色页与管理员页分工 | 角色页只有“选择绑定”，没有“新建资产” |

### 人工手动冒烟

| # | 用例 | 验证点 |
| --- | --- | --- |
| `H212` | 管理员新增一个数据资产并在角色页绑定 | 分工清楚，路径顺畅 |
