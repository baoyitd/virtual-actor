# v0.4.0 规划复审转发说明

用途：转发给工作方，说明当前最新复审结论。

## 结论

本轮前述 5 个阻塞项已复核通过，当前结论更新为：

```text
规划通过，可进入实现
```

## 已确认收口

1. `usage_records` 已统一为 `String(36)` UUID 主键 / 外键口径，与现有 `RoleAsset.id`、`RoleVersion.id` 一致。
2. `capability_level` 已明确为**版本级属性**，并收口 schema 写入、schema 回读、EAV 持久化、版本快照、前端类型、UI 写读和对外响应排除说明。
3. 竞品表述已收敛到“当前抽样竞品 / 已核对证据范围”的相对优势口径。
4. `DD-06 角色模板库` 已移出本轮 `Scope In`、验收标准、追溯矩阵和测试承诺。
5. `delivery/test-plan-v0.4.md` 遗留旧编号已修正，当前 dossier 编号一致。

## 质量门禁复核结果

以下命令已复跑通过：

1. `npm run lint:md`
2. `python3 scripts/iteration-guard.py --repo-root . --mode release`
3. `./venv/bin/python -m pytest tests -q`
4. `cd frontend && npm run build`

结果为：`markdownlint 0 errors`、`iteration guard passed`、`pytest 38 passed`、`frontend build passed`。

## 当前执行边界

1. 现在可以进入 `v0.4.0` 功能实现。
2. `Formal Status` 仍保持 `Draft`，不能因为规划通过就升级为 `Self-Tested`、`User-Acceptance-Candidate` 或 `Accepted`。
3. 如果实现过程中新增接口、改变公共对象、扩大 Scope In、修改读写边界或版本规则，必须先回写 dossier 再继续。
4. 不得混入决策产品集成实现或 `A3` 执行动作。

## 非阻塞建议

建议在进入代码实现前，为 `capability_level` 增补一条“**不进入 `RoleVersionPublicResponse`**”的显式测试用例，避免后续无意扩大对外响应范围。该项不阻塞进入实现。

## 权威复审记录

详细复审记录见：

`/Users/baoyi/Documents/code_buddy/virtual-actor/docs/iterations/v0.4.0/planning-review-2026-05-25.md`
