# v0.5.1 独立复审意见（致工作方）

> 来源：独立审核方（Independent Reviewer）
> 日期：2026-06-26
> 审核对象：v0.5.1 工作包（Knowledge Workbench 公共契约切换）
> 审核依据：`docs/iterations/v0.5.1/`（scope / design-delta / implementation-notes / traceability）、live 代码文件、`delivery/` 交付物
> 审核立场：不替执行方补圆方案，证据不足默认不能 Pass

---

## 一、审核结论

### **判定：CANNOT PASS（不通过）**

代码层面的核心变更已落地且可验证，但存在 **3 项 Blocker** 和 **3 项 Major** 级治理文档缺陷，导致无法确认"当前工作是否真的达到了该工作包目标"。在以下问题修复并补齐证据前，不得判定为通过。

---

## 二、Blocker 级问题（阻断通过）

### B-1：DD-30 接口基线文档未更新（P0 范围项未完成）

- **scope 要求**：DD-30「接口基线文档更新 — `role-to-knowledge-integration-proposal.md` 更新为 Knowledge Workbench 接口版本」，优先级 P0。
- **live 文件实况**：`docs/handoff/role-to-knowledge-integration-proposal.md` 仍为 **v0.3**，日期 **2026-05-14**，全部描述 Open WebUI 端点。
  - 第 1 行：`# 角色产品 → 知识平台 集成方案（v0.3 — MVP 就绪）`
  - 第 33 行：`GET /api/v1/knowledge/` → `GET /api/v1/knowledge/{kb_id}/files`
  - 第 49 行：`POST /api/chat/completions`
  - 第 57 行：`GET http://localhost:3000/api/v1/version`
- **影响**：P0 范围项明确未完成。该文档是角色产品与知识平台之间的集成契约基线，当前内容与实际实现完全脱节。

**要求**：将文档更新为 Knowledge Workbench `/api/public/*` 接口版本，反映当前实际端点契约（packages / manifest / status / retrieve / route）。

---

### B-2：追溯矩阵未更新，与发布状态直接矛盾

- **traceability.md 实况**：全部 14 个场景的结果证据列仍为「待联调」「待补充」「待验证」，发布口径列全部为「待验收」。
- **release-notes.md 实况**：`当前状态: Accepted`，声称「端到端联调通过」「retrieve scope 过滤验证通过」。

| 场景 | traceability 证据列 | traceability 发布口径 | release-notes 声称 |
|------|---------------------|----------------------|-------------------|
| US-31 | 待联调 | 待验收 | 端到端联调通过 |
| US-32 | 待联调 | 待验收 | 端到端联调通过 |
| US-34 | 待联调 | 待验收 | 端到端联调通过 |
| US-38 | 待验证 | 待验收 | — |
| US-41 | 待验证 | 待验收 | — |
| US-42 | 待验证 | 待验收 | — |
| US-43 | 待验证 | 待验收 | — |
| US-44 | 待验证 | 待验收 | — |

- **影响**：追溯规则 #3 明确要求「如果发布说明写了某项能力'已支持'，但没有实现和证据映射，不得对外表述为已完成。」当前 release-notes 声称 Accepted，但追溯矩阵无任何证据映射支撑。

**要求**：将 traceability.md 中全部 14 个场景的结果证据列和发布口径列更新为实际验证结果，补充证据文件路径或测试命令。

---

### B-3：v0.5.1 无测试用例文档，TC-31~TC-44 不存在

- **delivery 目录实况**：存在 `test-cases-v0.5.md`（v0.5.0 用例），但**不存在** `test-cases-v0.5.1.md` 或任何 v0.5.1 专属测试用例文档。
- **追溯矩阵引用**：US-31~US-44 分别引用 TC-31~TC-44，但这些测试用例 ID 在整个仓库中**不存在定义**（搜索 `TC-3[1-9]|TC-4[0-4]` 在 tests/ 目录返回 0 结果）。
- **test-results.md 实况**：顶部含 v0.5.1 自动化测试结果（44 passed），但结果只对应自动化命令输出，无法映射到具体测试用例。

- **影响**：无法确认"44 passed"覆盖了哪些 v0.5.1 范围项。没有测试用例定义，测试结果不可追溯，无法独立判断测试覆盖是否充分。

**要求**：创建 `delivery/test-cases-v0.5.1.md`，定义 TC-31~TC-44 的用例描述和验证点，使测试结果可追溯。

---

## 三、Major 级问题（严重影响可信度）

### M-1：停止条件治理缺口——代码切换未被文档授权

- **scope.md 停止条件 #1**：「若 Knowledge Workbench Open WebUI 适配器未就绪，不进行代码切换。」
- **scope.md 前置依赖**：「Knowledge Workbench Open WebUI 适配器就绪 — **未完成**」
- **implementation-notes.md 偏差记录**：「当前阻塞中」「Open WebUI 适配器就绪且通过知识平台侧内部测试后才启动代码切换」
- **live 代码实况**：`knowledge_platform.py` 已完成全部切换，调用 `/api/public/packages`、`/api/public/retrieve` 等端点。
- **release-notes.md**：声称「端到端联调通过」、状态 Accepted。

- **问题**：scope.md（06-17）和 implementation-notes.md 均未更新。两种可能：
  1. 适配器在 06-17~06-23 之间就绪，但停止条件的解除未被记录 → 治理文档未跟进
  2. 适配器仍未就绪，代码切换违反停止条件 → 停止条件被违反

  无论哪种情况，当前文档无法证明代码切换被授权。

**要求**：更新 scope.md 和 implementation-notes.md，记录 Open WebUI 适配器的实际就绪状态和代码切换的授权决策路径。若适配器仍未就绪，需明确说明代码切换的替代授权依据。

---

### M-2：release-notes 内部自相矛盾——M01/M02 同时"已关闭"和"仍是限制"

- **release-notes.md「当前限制」第 1-2 条**：
  1. 「React 详情页仍有少量中文体验问题：`decision_style`、`collaboration_mode` 会显示内部枚举值」
  2. 「已发布角色在进入新草稿后，React UI 仅保留历史版本摘要时间线，尚无独立历史版本详情入口」
- **known-issues.md「已关闭事项」**：
  - M01（同一问题）：「已关闭」——前端新增 `decisionStyleText`/`collaborationModeText` 映射表
  - M02（同一问题）：「已关闭」——`RoleVersions` 版本详情面板升级为结构化展示

- **问题**：同一问题在两份文档中状态相反。

**要求**：移除「当前限制」中已关闭的 M01/M02 条目，或在 known-issues.md 中将 M01/M02 重新标记为未关闭。二选一，保持一致。

---

### M-3：release-notes 测试 warnings 数量与 test-results 不一致

- **release-notes.md**：`44 passed, 10 warnings`
- **test-results.md（v0.5.1 主测试）**：`44 passed, 0 warnings`
- **test-results.md（M05 补丁验证，06-26）**：`44 passed, 0 warnings`（明确说明「M05 修复后 warnings 从 10 降为 0」）
- **known-issues.md**：`自动化质量门禁通过（44 passed，0 warnings）`

- **问题**：release-notes（06-23）写于 M05 修复前，M05 修复（06-26）后未回更 release-notes。当前 release-notes 的 warnings 数据已过期。

**要求**：将 release-notes.md 的 warnings 数量更新为 0，反映 M05 修复后状态。

---

## 四、已验证通过的范围项

以下范围项经 live 代码审查确认已实现：

| DD | 范围项 | 验证结果 | 证据位置 |
|----|--------|---------|---------|
| DD-25 | 接口对接点切换 | ✅ | `knowledge_platform.py` 全部调用 `/api/public/*` |
| DD-26 | 检索结果结构适配 | ✅ | `consume_service.py:543-559` `_build_sources` 含 tier/doc_role/evidence_type |
| DD-27 | 说明卡 tier 分布结构化描述 | ✅ | `role_service.py:1018-1022` 生成「N篇P1、M篇P2」格式 |
| DD-28 | 知识版本标识适配 | ✅ | `knowledge_platform.py:99-120` `get_package_status` + `current_version_id` |
| DD-29 | 健康检查适配 | ✅ | `knowledge_platform.py:261-263` `health()` 调用 `/api/public/packages` |
| DD-35 | business_domains + enterprise_roles 建表 | ✅ | `v051_governance_config.py` 迁移 + `config.py` CRUD API + 12 域 30 角色种子数据 |
| DD-36 | 消费回答两次调用机制 | ✅ | `consume_service.py:439-487` 第一次自由回答 + 第二次结构化提取 (temp=0.1) |
| DD-37 | source hash 移除 business_domain | ✅ | `briefing_service.py:78-108` payload 不含 business_domain |
| DD-38 | source hash 移除 validation_summary | ✅ | `briefing_service.py:78-108` payload 不含 validation_summary |
| DD-40 | 合并 05+06 为统一外供页 | ✅ | `RoleStageNav.tsx` 5 步导航；`UsageDesk.tsx` 已删除；`App.tsx` `/use` 重定向到 `/exports` |
| DD-41 | retrieve 端点 + knowledge_object_ids | ✅（有偏差） | `knowledge_platform.py:62-67` 使用 `/api/public/retrieve`（设计写 `/api/retrieve`，known-issues R19 记录了统一为 `/api/public/*` 前缀的决策） |

---

## 五、未充分验证的范围项（P1，需补 UI 级证据）

| DD | 范围项 | 当前状态 | 缺失证据 |
|----|--------|---------|---------|
| DD-31 | 03 测试页信息架构调整 | 代码存在 | 无 UI 级验证证据（仅「前端构建通过」） |
| DD-32 | 04 治理页信息架构调整 | 代码存在 | 无 UI 级验证证据 |
| DD-33 | 06 外供页信息架构调整 | 代码存在 | 无 UI 级验证证据 |
| DD-34 | 治理页表单交互变更 | 代码存在 | 无 UI 级验证证据 |
| DD-39 | 04 治理页操作区重构 | 代码存在 | 无 UI 级验证证据 |

以上 5 项代码层面已实现，但缺少前端 UI 级独立验证证据。traceability 矩阵均标为「待验证」，与实际发布状态矛盾。

---

## 六、修复清单（汇总）

修复完成后可重新提交复审。

### Blocker（必须）

1. **B-1**：更新 `docs/handoff/role-to-knowledge-integration-proposal.md` 为 Knowledge Workbench `/api/public/*` 接口版本。
2. **B-2**：更新 `docs/iterations/v0.5.1/traceability.md`，将 14 个场景的证据列和发布口径列填入实际结果。
3. **B-3**：创建 `delivery/test-cases-v0.5.1.md`，定义 TC-31~TC-44。

### Major（必须）

4. **M-1**：更新 `scope.md` 和 `implementation-notes.md`，记录 Open WebUI 适配器实际就绪状态和代码切换授权路径。
5. **M-2**：消除 release-notes 与 known-issues 关于 M01/M02 的状态矛盾。
6. **M-3**：同步 release-notes 的 warnings 数量为 0。

### 建议补充（非阻断）

7. 为 DD-31~DD-34、DD-39 五项 P1 前端变更补充 UI 级验证证据（截图或烟测记录），并回填至 traceability。

---

## 七、审核方声明

本报告基于 2026-06-26 对 live 代码文件和 `delivery/` 交付物的独立审查。审核方未参与方案制定和代码实现，仅基于提交包和验证证据进行独立判断。

代码实现质量整体可认可，核心接口切换、两次调用机制、source hash 修正、建表迁移等均已落地。但治理文档的严重不一致使当前提交包无法通过独立复审——**证据不足，默认不能 Pass**。

修复上述 6 项必须修复问题后可重新提交复审。
