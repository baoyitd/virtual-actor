# 规则变更记录

> **已整合**：规则变更索引已纳入 `project-rules.md` 底部"规则变更索引"章节。本文件保留作为历史参考，新增变更记录请同时更新 `project-rules.md`。
>
> 适用项目：`virtual-actor`
> 项目级规则真源：`docs/process/project-rules.md`
> 活跃版本真源：`docs/iterations/current.txt` + 活跃版本 `README.md` + 活跃版本 dossier + 活跃 README 列出的交付真源文件

| 日期 | 规则版本 | 变更 | 原因 | 影响 |
|---|---|---|---|---|
| 2026-05-22 | R-001 | 建立本项目迭代控制机制、版本 dossier、iteration guard、文档质量闸门和 Codex skill | v0.3.0 商业试用验收暴露出设计、测试、实现和交付证据需要更强一致性控制 | 后续版本必须先有 scope/design/test/traceability，再进入实现和验收表述 |
| 2026-05-28 | R-002 | 复审/整改要求是设计输入，不是已收口设计规格；任何来源的设计变更必须先写入 dossier 再进入实现 | DD-13 复审整改时将复审要求直接当作可实施规格，违反设计先行规则（scope.md §6/§11、product-iteration-control.md §3.2） | 复审退回、整改要求、规划方裁决等外部输入必须先回写设计文档，再进入代码实现；"先改代码后补文档"不属于设计先行流程 |
| 2026-06-17 | R-003 | 新增两条操作原则：(1) 论证从本质/项目目标/用户场景出发推导结论，不为已有成果找理由；(2) 收到指令后先反馈理解、异议、优化方案，不默认执行 | 项目接手阶段与产品方对齐操作方式，确保后续工作方不会以已有实现作为合理性辩护依据，也不会默认接受指令后直接执行 | 讨论设计/实现合理性时必须从本质推导；收到指令必须先反馈理解→异议→优化→确认后执行 |
| 2026-06-17 | R-004 | 新增 A-09 UI调整与设计变更的判定边界规则：纯视觉/样式调整不需要走设计变更流程；信息架构变化、核心动作位置变化、交互元素增减、任务流程变化属于设计变更必须走流程；不确定时默认按设计变更处理 | 03/04/06 页面布局调整直接改了代码未先回写真源设计，违反设计先行原则；需要明确边界让后续 UI 类改动有判定依据 | 任何涉及信息架构、核心动作位置、交互元素增减、任务流程的前端改动必须先回写 design-delta.md → 补对应用例 → 再改代码；纯视觉调整（颜色/间距/字体/图标/文案微调）可直接执行 |
| 2026-06-26 | R-006 | 规则体系适配 v0.5.1：(1) 修复 A 章编号冲突——第二个 A-07 改为 A-10；(2) 新增 A-11 外部反馈不默认执行规则（用户补充）；(3) D-01/F-03/L-07 适配 DD-14 决策产品集成暂缓状态；(4) G 章"v0.4 不实现"改为"当前不实现"；(5) M 章标记为已完成/历史参考；(6) 强制读取顺序修复重复读取 scope.md；(7) K-01 标注已实现，IO-004 关闭 | handoff 接手后发现 project-rules.md 仍绑定 v0.4 口径，与当前 v0.5.1 Accepted 状态不一致；DD-14 已正式暂缓但规则仍写"不得移出"；A 章有两个 A-07 编号冲突；用户补充外部反馈处理规则 | A 章编号无冲突；外部反馈有明确处理流程；DD-14 口径与 release-notes 一致；G 章不再绑定特定版本；M 章不混淆当前规则与历史规则 |
